version: '3'
services:
  banco-teste:
    container_name: banco-docker
    image: postgres
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: teste
      POSTGRES_DB: quarkus-docker

